#include <string.h>
#include <stdio.h>
using namespace std;
void RMQ(int num)
{
    int i,j;
    for(j=1;j<20;j++)
        for(i=1;i<num;i++)
        if(i+(1<<j))
    {

    }
}
int main()
{
    int N,Q,m,n;
   scanf("%d%d",&N,&Q);
   for(i=0;i<N;i++)


    while(Q--)
   {
       scanf("%d%d",&m,&n);

   }
    return 0;
}
